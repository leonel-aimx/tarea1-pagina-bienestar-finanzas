# AI Agent Instructions

## Purpose
This repository currently has no source files or existing project configuration.

## How to behave
- Ask the user what type of project they want to create before making assumptions.
- If the user requests scaffolding, propose the minimal files needed for the chosen stack.
- Do not invent a full architecture without explicit user direction.
- Once files exist, update this document with any discovered build/test commands, project conventions, or architecture notes.

## When new files are added
- Look for common project markers such as `package.json`, `pyproject.toml`, `requirements.txt`, `gradle.build`, `pom.xml`, `README.md`, or directories like `src/`, `app/`, `backend/`, `frontend/`.
- Prefer linking to existing documentation if present.
- Keep additional instructions concise and avoid duplication.

## Notes for future customization
- If this becomes a multi-language project, consider splitting instructions into domain-specific sections.
- Add build/test commands once the project structure is known.
